name: Scheduled Tests (disabled)

# Removed — tests run locally and on merge to main (ci.yml)
on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Tests run on push to main via ci.yml"
